# Reflex — Timing Log

## Technical dry run (backend, measured directly)
Confirms the system itself has no performance problem hiding under the demo — the full retailer→dispatcher→rider→delivered lifecycle, API-only, no human narration:

| Step | Time |
|---|---|
| Create delivery | 33ms |
| Assign to rider | 10ms |
| Mark picked up | 10ms |
| Confirm delivered | 11ms |
| **Total round-trip** | **66ms** |

This confirms the app itself is not what will eat your 10-minute slot — any time pressure in the live demo is about pacing your narration, not waiting on the system.

## Presentation dry runs (fill in as you rehearse — brief requires at least two)

### Dry run 1 — timing only, no scoring (Day 2, per the brief's schedule)
- Date/time run:
- Total time (target: 10 min presentation):
- Section timings:
  - Problem:
  - Solution:
  - Architecture:
  - Trade-offs:
  - Roadmap:
  - Live demo:
- Slides that felt unclear or rushed:
- One thing to fix before the next run:

### Dry run 2 — mock panel session (Day 3, per the brief's schedule)
- Date/time run:
- Total time:
- Section timings: (same breakdown as above)
- Cross-exam questions you got that you hadn't prepared for:
- Weak defenses flagged by the panel/peers:
- One thing to fix before submission:

### Notes for filling this in
- Time yourself with a stopwatch app on your phone, section by section — not just total time, since the brief specifically asks you to identify *which slide* is running long, not just whether the overall pitch does.
- If a section runs long twice in a row, that's a sign the slide is doing two jobs (per the brief's "one takeaway per slide" rule) — split it rather than trying to talk faster.
